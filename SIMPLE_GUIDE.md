# আয়-ব্যয় Feature Main Dashboard এ যোগ করার সহজ গাইড

## ✅ ইতিমধ্যে সম্পন্ন:

1. ✅ "আয়-ব্যয়" 💰 page sidebar এ যোগ হয়েছে
2. ✅ `format` import যোগ হয়েছে
3. ✅ jsPDF install হয়েছে

---

## 📝 এখন মাত্র ২টি কাজ করতে হবে:

### কাজ ১: Finance Content Render করুন

`backend/app/(dashboard)/dashboard/page.tsx` file খুলুন এবং:

**Line 437 এর পরে** (যেখানে `{activePage === "control" && <ControlDashboardPage />}` আছে)

এই line টি যোগ করুন:

```tsx
{
	activePage === "finance" && <FinanceContent />;
}
```

অর্থাৎ line 437 এর পরে এভাবে হবে:

```tsx
{
	activePage === "control" && <ControlDashboardPage />;
}
{
	activePage === "finance" && <FinanceContent />;
}
```

---

### কাজ ২: FinanceContent Component যোগ করুন

**Same file এর একদম শেষে** (line 3413 এর ঠিক আগে, যেখানে `export default function Dashboard()` আছে সেখানে)

`FINANCE_COMPONENT.tsx` file এর সব code copy করে paste করুন।

---

## 🎯 সম্পূর্ণ!

এখন browser এ যান:

1. **Refresh করুন** (Ctrl+R)
2. Sidebar এ **"আয়-ব্যয়" 💰** দেখতে পাবেন
3. Click করলে আয়-ব্যয় management page খুলবে!

---

## ✨ Features:

✅ আয় যোগ করুন
✅ ব্যয় যোগ করুন  
✅ মাসিক/বার্ষিক ভিউ toggle
✅ তারিখ রেঞ্জ ফিল্টার
✅ PDF ডাউনলোড

---

## 📂 Files:

- `FINANCE_COMPONENT.tsx` - Component code
- `FINANCE_DASHBOARD_GUIDE.md` - বিস্তারিত guide
- `backend/app/(dashboard)/dashboard/page.tsx` - Main file যেখানে edit করতে হবে

---

## ❓ সমস্যা হলে:

1. Browser console check করুন (F12)
2. Backend terminal check করুন
3. File save হয়েছে কিনা check করুন
